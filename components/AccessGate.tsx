'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
export default function AccessGate({project}:{project:any}){const [name,setName]=useState(''),[email,setEmail]=useState(''),[age,setAge]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false),router=useRouter();const valid=name.trim().length>1&&/^\S+@\S+\.\S+$/.test(email)&&age;async function submit(e:any){e.preventDefault();if(!valid)return;setBusy(true);setError('');const r=await fetch('/api/access',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,email,ageConfirmed:age,projectId:project.id})});if(r.ok)router.refresh();else{setError((await r.json()).error||'Unable to record access.');setBusy(false)}}return <main className="gate"><section className="gate-panel"><div className="eyebrow">Private viewing · 18+</div><h1>{project.title}</h1><p className="gate-copy">This project contains mature imagery. Your name and email are requested for visitor verification and access records. You are not being subscribed to a mailing list.</p><form className="gate-form" onSubmit={submit}><label>Name<input required type="text" value={name} onChange={e=>setName(e.target.value)} autoComplete="name"/></label><label>Email address<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><label className="check"><input type="checkbox" checked={age} onChange={e=>setAge(e.target.checked)}/><span>I confirm that I am 18 or over and understand that this project may contain artistic nudity or mature imagery.</span></label>{error&&<p className="error">{error}</p>}<button className="enter" disabled={!valid||busy}>{busy?'Recording access…':'Enter project'}</button></form><p className="humour">You accept that you may encounter nude images of your best mate, someone you went to school with, or someone you really weren't expecting to see naked.</p></section></main>}
