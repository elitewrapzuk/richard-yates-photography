import { redirect } from 'next/navigation'
import { createServerClient } from '@/lib/supabase'
export default async function Admin(){const s=await createServerClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/admin/login');const [{count:projects},{count:published},{count:visitors},{data:recent}]=await Promise.all([s.from('projects').select('*',{count:'exact',head:true}),s.from('projects').select('*',{count:'exact',head:true}).eq('status','published'),s.from('visitor_access').select('*',{count:'exact',head:true}),s.from('visitor_access').select('name,email,created_at,projects(title)').order('created_at',{ascending:false}).limit(8)]);return <main className="admin"><header className="site-header"><a href="/">RICHARD YATES</a><a href="/admin/logout">Sign out</a></header><p className="eyebrow">Private studio</p><h1>Overview</h1><div className="admin-grid"><div className="stat">Projects<strong>{projects||0}</strong></div><div className="stat">Published<strong>{published||0}</strong></div><div className="stat">Visitors<strong>{visitors||0}</strong></div><div className="stat">Access records<strong>Live</strong></div></div><h2>Recent visitors</h2><table className="admin-table"><thead><tr><th>Name</th><th>Email</th><th>Project</th><th>Date</th></tr></thead><tbody>{(recent||[]).map((v:any)=><tr key={v.created_at+v.email}><td>{v.name}</td><td>{v.email}</td><td>{v.projects?.title}</td><td>{new Date(v.created_at).toLocaleString()}</td></tr>)}</tbody></table></main>}
