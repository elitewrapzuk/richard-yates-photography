import { createServerClient } from '@/lib/supabase';import { redirect } from 'next/navigation'
export default function Login(){async function login(formData:FormData){'use server';const s=await createServerClient();const {error}=await s.auth.signInWithPassword({email:String(formData.get('email')),password:String(formData.get('password'))});if(error)redirect('/admin/login?error=1');redirect('/admin')}return <main className="gate"><section className="gate-panel"><div className="eyebrow">Private studio</div><h1>Admin login</h1><form className="gate-form" action={login}><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required/></label><button className="enter">Sign in</button></form></section></main>}
